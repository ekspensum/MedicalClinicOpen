import { DoctorFreeDayData } from './doctor-free-day-data';

describe('DoctorFreeDayData', () => {
  it('should create an instance', () => {
    expect(new DoctorFreeDayData()).toBeTruthy();
  });
});
