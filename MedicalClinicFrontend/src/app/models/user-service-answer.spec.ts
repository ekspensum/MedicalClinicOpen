import { UserServiceAnswer } from './user-service-answer';

describe('UserServiceAnswer', () => {
  it('should create an instance', () => {
    expect(new UserServiceAnswer()).toBeTruthy();
  });
});
